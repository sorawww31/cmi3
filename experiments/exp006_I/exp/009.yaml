# Full training experiment
defaults:  
  - default@_here_  # defaultの値を設定してから上書きする

seed: 42
folds: [0]
epochs: 100
learning_rate: 3e-4
weight_decay: 3e-4
batch_size: 64
rnn_type: "transformer"
rnn_num_layers: 2
branch_hidden_multiplier: 2
features: ["imu","rot", "angular_vel", "jerk"]
patience: 20